using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Player_State { IDLE, MOVE, ATTACK, ATTACK2, Sild, JUMP, DIE, Cast }

public enum Enemy_State { MOVE, ATTACK , HIT}

public enum Boss_State { IDLE, MOVE , ATTACK, DIE, CAST, TELEPORT}
